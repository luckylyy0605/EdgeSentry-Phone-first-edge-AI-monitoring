# Mobile Monitor — Claude Code 工作约定

## 项目身份

手机端 GUI + 外设采集 + 端侧 AI 推理监控系统。手机只负责交互和媒体采集/播放，模型继续运行在 PC 或开发板。

## 开始任何任务前，必须先读

1. `task_plan.md`
2. `findings.md`
3. 根目录 `progress.md`
4. 当前版本/平台下的 `AGENTS.md` 与 `progress.md`
5. 当前单元涉及的 `docs/` 和 `configs/`

## 开发规则

- 每次只执行 `roadmap.md` 中一个编号单元，例如 1.1；不得跨单元一次完成。
- 编码前先写清假设、输入输出和验收命令。
- 完成后运行该单元的验证命令，通过后更新两个 `progress.md`；这两个文件就是本项目的工作日志，禁止只在对话里汇报而不落盘。
- 禁止把模型权重、测试视频、证据产物、密钥和设备证书提交到 git。
- 所有设备地址、模型路径、分辨率、帧率和阈值必须来自配置。
- 网络断开、权限拒绝、推理失败、帧丢弃必须显式显示，禁止静默失败。

## 工作日志要求

每次 Claude Code 会话结束前，必须在当前平台的 `progress.md` 追加一条带日期时间的记录，并在根目录 `progress.md` 更新项目级摘要。记录至少包含：

- 本次只处理的编号单元；
- 实际新增/修改的文件；
- 执行过的验证命令及结果；
- 未完成、跳过、失败或仅人工验证的项目；
- 下一步建议，但不得提前实现下一单元。

若本次没有修改代码，也要记录“只调研/只诊断/只设计”及结论。不得伪造未执行的测试结果。

## Git 授权规则

- Claude Code 可以执行只读 Git 命令，例如 `git status`、`git diff`、`git log`，用于检查改动。
- 需要创建分支、`git add`、commit、push、merge、rebase、tag 或修改远端之前，必须先展示待操作范围，并明确询问用户是否执行。
- 即使某个单元已验证通过，也只能建议 commit，不能自动 commit。
- 用户同意 commit 后，先显示 `git status --short` 与变更摘要，再使用：`feat(1.1): 描述`、`fix(1.1): 描述` 或 `docs(1.1): 描述`。
- 用户对一次 Git 操作的同意不自动授权后续 push、merge 或下一个单元的 commit；每个有外部影响的动作分别询问。
- 禁止把与当前编号单元无关的用户改动加入暂存区。

建议询问格式：

```text
单元 1.X 已完成并通过：<验证摘要>。
当前有 <N> 个相关文件发生变化：<摘要>。
是否现在为这些相关文件创建 Git commit？建议消息：<message>
```

## 交互页面实施原则

- v1 采用响应式 HTML/PWA，实现手机浏览器直接使用；不要同时创建原生 Android/iOS App。
- 严格按 `v1/pc/docs/roadmap.md` 的 1.1 → 1.9 顺序开发交互页面与后端。
- 实时监控页优先完成可用性：本地预览、连接状态、开始/停止、摄像头切换、麦克风开关、叠加结果和故障提示。
- 任何视觉优化都不能绕过协议、限流、错误状态和手机实机验收。

## 边界

- GUI 不直接拼接 shell 命令。
- API 层不决定模型类别或推理逻辑；只负责鉴权、会话、限流、路由和协议转换。
- 推理 adapter 只实现固定接口；真实模型、mock、回放必须可以替换。
- 实时路径不得无限排队；过期帧直接丢弃并上报统计。
- 在 1.9 之前不得把 `v6demo` 整体复制到新项目；需要复用的内容先登记，完成界面闭环后再按迁移清单选择性复制。
