# Mobile Monitor 方案规划

## 目标

为 `v6demo` 设计一个独立、可版本化演进的手机端监控与外设接入项目：手机提供 GUI、摄像头、麦克风和扬声器，开发板继续负责模型推理；首版先在 PC 完成端到端验证，再迁移到 RK3568/Ascend。

## 成功标准

- 新项目位于 `Mobile_Monitor/`，不复制或侵入式修改 `v6demo`。
- 明确手机、服务端、板端适配器三者的职责与通信协议。
- 明确视频/音频链路以及“解码问题”能解决到什么程度。
- 给出按编号单元推进的版本路线、验收标准和 Claude Code 工作方式。
- 按项目规范创建首个 PC 平台目录、`AGENTS.md` 与 `progress.md`。

## 假设

- 手机与开发板通常处于同一局域网，公网远程访问不是 v1 范围。
- 手机端优先采用浏览器/PWA，避免 v1 同时维护 Android/iOS 原生应用。
- `v6demo` 当前可以通过 CLI 启动推理，并能产生日志、检测结果或结构化证据。
- 手机摄像头视频先由手机浏览器采集；板端只接收已协商的标准编码流或抽帧图像。
- 现阶段以单手机、单板、单任务为最小闭环。
- `v6demo` 的有用资产在 PC 交互闭环完成后再整理迁移，现阶段不批量复制。

## 阶段

| 阶段 | 状态 | 内容 |
|---|---|---|
| 1 | complete | 审计 `v6demo` 的入口、输入输出、依赖与已知问题 |
| 2 | complete | 确定总体架构、协议、解码策略与安全边界 |
| 3 | complete | 设计目录结构与 v1/v2/v3 路线 |
| 4 | complete | 创建新项目文档骨架和 Claude Code 开发约束 |
| 5 | complete | 校验文档一致性并给出实施建议 |

## 关键决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 项目名 | `Mobile_Monitor`（工作名称） | 描述手机监控用途，且独立于 `v6demo` |
| v1 手机形态 | PWA/Web GUI | 一个代码库覆盖 Android/iOS，摄像头/麦克风权限可用 |
| 项目关系 | 独立项目 + 板端 adapter | 避免 GUI 直接调用 CLI 或复制模型代码 |
| 画面显示 | 原始预览 + JSON/Canvas 叠加 | 避免板端持续软编码标注视频 |
| v1 上行 | 低帧率 JPEG/WebP 帧 | 最低集成风险，先验证手机到推理闭环 |
| v2 上行 | WebRTC/H.264 | 利用手机硬编，改善带宽与实时性 |
| 实时队列 | latest-frame-wins 有界队列 | 防止推理落后时延迟无限累积 |
| 坐标协议 | 0–1 归一化 bbox | 适配不同手机画布尺寸与旋转 |
| v6 实时接入 | 常驻 worker + Unix socket | 模型只加载一次，项目间保持协议边界 |
| 完全零改 v6 | 仅保留离线 CLI adapter | 分段反复启动 CLI 不满足实时要求 |
| 客户端形态 | v1 使用响应式 PWA | 相机/麦克风和跨平台需求已覆盖，开发与分发成本最低 |
| v6 资产迁移 | 1.9 按 manifest 选择性复制 | 先冻结新接口，避免把旧单体结构和无关资产搬入 |
| Git 操作 | 每次单独询问授权 | 只读检查可自动执行，写操作由用户决定 |

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| `git status` 无法读取用户级全局 ignore 文件 | 1 | 不影响仓库状态读取；已确认 `Mobile_Monitor/` 整体为未跟踪，未执行任何 Git 写操作 |
